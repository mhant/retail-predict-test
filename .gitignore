# Wrangler local cache — contains account metadata, not for version control
.wrangler/

# Python
__pycache__/
*.pyc
.env

# Models are committed intentionally (small XGBoost JSON, ~5MB each)
# Do NOT add models/ here
